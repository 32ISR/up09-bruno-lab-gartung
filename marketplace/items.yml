info:
  name: items
  type: http
  seq: 3

http:
  method: GET
  url: https://kitek.ktkv.dev/marketplace/api/items
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
