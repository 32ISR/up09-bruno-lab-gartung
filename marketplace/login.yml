info:
  name: login
  type: http
  seq: 2

http:
  method: POST
  url: https://kitek.ktkv.dev/marketplace/api/auth/login
  body:
    type: json
    data: |-
      {
        "username": "testuser10",
        "password": "password1234"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
